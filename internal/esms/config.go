package esms

type Config struct{}

func (c *Config) getIntConfig(name string, defaultValue int) int {
	// Placeholder implementation, replace with actual config retrieval logic
	return defaultValue
}
